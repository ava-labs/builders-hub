// Note: createMetadataImage was removed in fumadocs v16
// If metadata images are needed, use the new API from fumadocs-ui
import { documentation } from "@/lib/source";

export const metadataImage = {
  source: documentation,
  imageRoute: "og",
};
