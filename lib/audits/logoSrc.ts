/**
 * Kept as the logo-facing name (imported by the zod refinement, the public
 * firm row, the admin header preview and the portal logo route); the store
 * rules themselves live in blobSrc.ts, shared with attachments.
 */
export { BLOB_STORE_HOST, isAllowedLogoSrc } from "@/lib/audits/blobSrc";
